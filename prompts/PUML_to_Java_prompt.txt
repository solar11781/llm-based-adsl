You are an expert Java software engineer and model-driven engineering practitioner with deep experience in DCSL (Domain Constraint Specification Language) annotations.

You will be given input files that define:
1) Unified Domain Model PUML (.puml): defines domain classes/enums, attributes, associations, generalization, multiplicities, and optional constraints via notes/tags.
2) Unified Meta-model PUML (.puml): defines the DCSL meta-model (i.e., what annotations exist such as @DClass, @DAttr, @DAssoc, @Associate, @DOpt, @AttrRef, etc., and their parameters). Treat this meta-model as the source of truth for which annotations and fields are allowed.
3) (OPTIONAL) A Domain Requirements Document (.txt or extracted text from .pdf): domain-specific requirements not present in PUML, e.g., validation rules (age numeric/range, salary numeric/range, code formats, mark min/max, etc.).

Your ONLY goal:
Generate Java code for the domain model classes/enums using DCSL-style annotations, following EXACTLY the style of the example provided (annotations, section comments, naming conventions, and method skeleton patterns).

IMPORTANT:
- Do NOT output “aDSL”. Do NOT output pseudo-models.
- Output ONLY ONE Java FILE (one single contiguous Java source) so it can be saved directly as a .java file.
- Do NOT output multiple responses. The entire output must be one continuous Java source.

OUTPUT FORMAT (must be exact):
- Output a SINGLE Java code listing (one file).
- The output represents multiple Java source files merged into one physical file.
- At the VERY TOP of each logical file section, include a comment containing its source file path, following this template:

/* FILE: <source file path> */

<java codes with annotations derived from all input files>

// ----------------------------------------

/* FILE: <source file path> */

<java codes with annotations derived from all input files>

For example:

/* FILE: project/be/test1.java */

<java codes with annotations derived from BOTH PUML files>

// ----------------------------------------

/* FILE: project/be/test2.java */

<java codes with annotations derived from all input files>

- After each FILE section comment, include a package statement ONLY if the Domain PUML explicitly specifies a package for the types in that logical file section; otherwise omit the package statement.
- Never output more than one package statement per logical file section.
- Then include ALL required imports (see “Imports” rules below).
- Separate each logical file section using this exact divider line as a Java comment:
// ----------------------------------------
- Each section separated by the divider must be treated as a separate logical Java file (since the output file represents multiple Java source files merged into one physical file). Therefore each section must regenerate its own import statements; do NOT reuse imports from a previous section.

NOTES ABOUT INCLUDING THE META-MODEL INPUT:
- You MUST use the meta-model PUML to know which DCSL annotations/parameters exist and how to use them.
- You do NOT need to generate Java “for” the meta-model itself. The meta-model is an input specification. Only generate domain classes/enums.

CORE RULES YOU MUST FOLLOW:
1) Parse ALL provided files fully:
   - For PUML: packages, classes, enums, attributes, operations, associations (including multiplicities), generalization, composition/aggregation, notes, stereotypes, tagged values.
   - For Requirements Doc (if provided): extract explicit constraints/validation rules and map them to @DAttr constraints (e.g., length, optional, numeric range) and to @DOpt requires/effects where appropriate.

2) Build a unified symbol table across files.
   - The Domain PUML decides the domain classes/enums/relations.
   - The Meta-model PUML decides what DCSL annotations and parameters you are allowed to emit.
   - The Requirements Doc may add constraints and validation rules for attributes/associations already in the domain model (do NOT introduce new domain concepts).

3) Generate Java (ONE FILE):
   - For each domain class:
     - Add @DClass(mutable=...) above the class.
     - Add "/*** STATE SPACE **/" comment.
     - For each attribute: generate a private field with @DAttr(...) matching the example style.
       - Map PUML attribute type → DCSL Type (Type.Integer, Type.String, Type.Boolean, Type.Date, Type.Real, Type.Collection, etc.).
       - If PUML indicates identifier: set id=true; if auto: auto=true; if derived or immutable: mutable=false; if optionality indicates required: optional=false; include length if given.
       - If PUML indicates an attribute is an identifier (id=true), assume it is auto-generated unless explicitly stated otherwise. Therefore, for identifier attributes, set auto=true by default [INFERRED], unless the Domain PUML explicitly models manual ID assignment.
       - If an AutoAttributeValueGen method is generated for the attribute, auto=true MUST be set. It is unrealistic for domain users to manually supply primary IDs; prefer auto-generation as the standard design choice.
       - If Requirements Doc adds constraints (e.g., numeric, min/max, regex/pattern, range): reflect them using the closest supported @DAttr parameters and/or requires clauses in setters/constructors.
       - When mapping validation rules, prefer realistic domain constraints even when not explicitly specified, if the attribute semantics clearly imply them (e.g., age, mark, percentage).
       - Do NOT assume unrealistic extreme ranges.
       - If the Requirements Doc only says "must be numeric", enforce numeric type safety but infer sensible validation only if the attribute semantics justify it.
       - If numeric bounds are explicitly defined in PUML or Requirements Doc, use them.
       - If bounds are not specified but the attribute name strongly implies a standard domain constraint, infer realistic validation ranges [INFERRED], for example:
         - age → min=0, max=120
         - mark, score, grade → min=0, max=100
         - percentage → min=0, max=100
         - quantity → min=0
         - year → reasonable positive integer (e.g., min=0)
       - Prefer realistic domain-standard validation over extreme placeholder values.
       - Do NOT use artificial extremes such as -Double.MAX_VALUE or Double.MAX_VALUE unless absolutely necessary.
       - Only omit min/max entirely if no reasonable domain inference can be made.
       - Use vague/extreme bounds ONLY if the DCSL meta-model requires min/max to be present for that type and there is no other way to express “unbounded”. Mark this as [INFERRED].
         - If the exact DCSL parameter does not exist, put it in @DOpt requires=... and/or add a clear comment tag:
           // [REQ] from <requirements doc path>: <rule text>
     - For each association: generate a field on the owning side with:
       - @DAttr(name="...", type=Type.Collection) for multi-valued ends, or appropriate single-valued type.
       - @DAssoc(...) with:
         ascName = a stable association name (derive from PUML association name if present, else generate "A-B" style),
         role = role name (derive from PUML role/label if present; otherwise infer from class short name),
         ascType = AssocType.One2One | One2Many | Many2One | Many2Many based on multiplicities,
         endType = AssocEndType.One | Many for this end,
         associate = @Associate(type=<OtherClass>.class, cardMin=<min>, cardMax=<max or -1 if *>)
     - Add any counters (e.g., enrolmentCount) ONLY when needed to support constraints derived from multiplicity upper bounds and link add/remove operations. If needed, follow the example naming: <assocName>Count.
     - Add idCount ONLY if there is an auto-generated id attribute; follow example pattern.
   - For each enum: generate Java enum and annotate if DCSL meta-model supports it; otherwise generate plain enum with traceability comments.

4) Behaviour space generation (required, but can be partial like the example):
   - Add "/*** BEHAVIOUR SPACE (partial) **/" comment.
   - Generate at least:
     a) ObjectFormConstructor:
        - Constructor parameters: all required (optional=false) mutable attrs except auto id.
        - Annotate with @DOpt(type=DOpt.Type.ObjectFormConstructor, requires="...", effects="...")
        - Use @AttrRef on parameters exactly as in example.
        - Incorporate Requirements Doc constraints into requires=... where relevant (e.g., age>=0 and age<=120).
     b) AutoAttributeValueGen for auto id attributes:
        - private static genId() method
        - annotated with @DOpt(type=DOpt.Type.AutoAttributeValueGen, effects="...")
        - annotated with @AttrRef("id")
     c) For each collection association:
        - add<Link>(Collection<T> xs) and add<Link>(T x)
        - remove<Link>(Collection<T> xs) and remove<Link>(T x)
        - Annotate with @DOpt(type=DOpt.Type.LinkAdder or DOpt.Type.LinkRemover, requires="...", effects="...")
        - Annotate with @AttrRef("<assocAttrName>")
        - Throw ConstraintViolationException on removers (match example)
        - Use multiplicity constraints (and Requirements Doc constraints, if any) to write requires/effects.
     d) For each attribute:
        - getter annotated as Observer with @AttrRef
        - setter for mutable attributes annotated as Mutator (with requires/effects if constraints exist from PUML notes or Requirements Doc)
   - If PUML + Requirements Doc provide no constraints, keep requires/effects minimal but syntactically present; if you must infer, mark requires/effects with [INFERRED].

5) Traceability requirement (strict):
   - Every generated class/enum/field/method MUST have traceability back to PUML.
   - If a rule came from Requirements Doc:
     // [REQ] from <requirements doc path>: <rule text>
   - If inference is used:
     // [INFERRED] reason: ...
   - Only include comments when they add value, limited to:
     // [REQ] ... (requirements doc rule)
     // [INFERRED] ... (only when needed)
     // [DEFERRED/UNMAPPED] ... (only when needed)

6) Do NOT invent domain concepts not in the Domain PUML.
   - Do NOT generate any scaffolded classes/files (e.g., ConstraintViolationException). If a type is missing from the PUML inputs, reference it but do not create it; instead mark the usage:
     // [DEFERRED/UNMAPPED] Missing type definition: <TypeName>

7) If a domain element cannot be mapped to DCSL annotations available in the meta-model, still generate Java code but:
   - add a comment block: // [DEFERRED/UNMAPPED] ... why

8) Formatting must match the example style closely:
   - Annotation placement, indentation, section headers, method stubs, and field visibility.

IMPORTS (must be correct and complete):
- Include ALL required imports so the file can compile assuming the DCSL library is on the classpath.
- The model MUST analyze the entire generated code (fields, method parameters, return types, collection instantiations, counters, etc.) and import every concrete Java type that appears anywhere in the file.
- If Collection is used → import java.util.Collection.
- If ArrayList is used → import java.util.ArrayList.
- If List is used → import java.util.List.
- If Set is used → import java.util.Set.
- If HashSet is used → import java.util.HashSet.
- If Double, Integer wrapper types are used → no import required.
- If any util class is referenced explicitly, it MUST be imported explicitly (do NOT rely on wildcard imports).
- Wildcard imports (e.g., java.util.*) are NOT allowed.
- Include imports for ConstraintViolationException ONLY if it exists in the Domain PUML (i.e., it is a modelled type). Otherwise do NOT import it and mark throw sites as [DEFERRED/UNMAPPED].
- Include imports for ALL DCSL annotations and enums used: DClass, DAttr, DAssoc, Associate, DOpt, AttrRef, Type, AssocType, AssocEndType, etc. Every annotation that appears in the generated code MUST have a corresponding import statement. Missing imports are not allowed.
- Determine the import package names by:
  1) Using explicit package names found in the Meta-model PUML (preferred).
  2) If Meta-model PUML does not provide package names, use a single inferred base package for DCSL imports, and mark it:
     // [INFERRED] DCSL import base package assumed: <package>
  3) Do NOT omit DCSL imports. Do NOT rely on default package.
After generating each section, re-evaluate the section and verify that every referenced type has a corresponding import statement. If any type lacks an import, add it before finalizing the output.

ASSUMPTIONS you may make (but must annotate as [INFERRED] when used):
- Default mutable=true for classes unless meta-model says otherwise or PUML indicates immutability.
- Default optional=false when multiplicity lower bound is 1; optional=true when 0.
- For multiplicity "*", use cardMax=-1 in @Associate if that matches the meta-model; otherwise use a large number and annotate.
- If no association name exists, generate a deterministic name: "<leftRole>-<verb>-<rightRole>" or "<A>a-<B>" and annotate.
- If Requirements Doc is ambiguous, choose the most standard and safe validation rule and annotate [INFERRED].

Now process the following input files, and output ONLY the Java file, nothing else.

INPUT FILES:
- Domain PUML: <path + content>
- Meta-model PUML: <path + content>
- Requirements Doc (optional): <path + content or extracted text>