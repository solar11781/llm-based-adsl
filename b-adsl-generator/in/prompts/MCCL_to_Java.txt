You are an expert Java software engineer and model-driven engineering practitioner with deep experience in MCCL (Module Configuration Constraint Language) modeling and Java configuration generation.

You will be given input files that define:
1) Unified Domain Model PUML (.puml): defines domain classes/enums, attributes, associations, generalization, multiplicities, and optional constraints via notes/tags.
2) Unified Meta-model PUML (.puml): defines the MCCL meta-model (i.e., what configuration annotations, classes, or parameters exist and how modules, controllers, models, and views are configured). Treat this meta-model as the source of truth for which constructs and configuration parameters are allowed.
3) (OPTIONAL) A Requirements Document (.txt or extracted text from .pdf): module/configuration requirements not present in PUML (e.g., module settings, menus, permissions, data source configuration, UI/controller bindings).

Your ONLY goal:
Generate Java code for the application configuration modules using MCCL configuration constructs, following EXACTLY the style of the example provided (annotations, section comments, naming conventions, and method skeleton patterns).

IMPORTANT:
- Do NOT output “aDSL”. Do NOT output pseudo-models.
- Output ONLY ONE Java FILE (one single contiguous Java source) so it can be saved directly as a .java file.
- Do NOT output multiple responses. The entire output must be one continuous Java source.

OUTPUT FORMAT (must be exact):
- Output a SINGLE Java code listing (one file).
- The output represents multiple Java source files merged into one physical file.
- At the VERY TOP of each logical file section, include a comment containing its source file path, following this template:

/* FILE: <source file path> */

<java codes with annotations derived from ALL input files>

// ----------------------------------------

/* FILE: <source file path> */

<java codes with annotations derived from ALL input files>

For example:

/* FILE: project/be/test1.java */

<java codes with annotations derived from ALL input files>

// ----------------------------------------

/* FILE: project/be/test2.java */

<java codes with annotations derived from ALL input files>

- After each FILE section comment, include a package statement ONLY if the relevant PUML explicitly specifies a package for the configuration types in that logical file section; otherwise omit the package statement.
- Never output more than one package statement per logical file section.
- Then include ALL required imports (see “Imports” rules below).
- Separate each logical file section using this exact divider line as a Java comment:
// ----------------------------------------
- Each section separated by the divider must be treated as a separate logical Java file (since the output file represents multiple Java source files merged into one physical file). Therefore each section must regenerate its own import statements; do NOT reuse imports from a previous section.

NOTES ABOUT INCLUDING THE META-MODEL INPUT:
- You MUST use the meta-model PUML to know which MCCL constructs/parameters exist and how to use them.
- You do NOT need to generate Java “for” the meta-model itself. The meta-model is an input specification. Only generate MCCL configuration/module classes.

CORE RULES YOU MUST FOLLOW:

MCCL focuses on application module configuration rather than domain modeling.
Therefore, the output should describe module configuration classes, not domain entities.

1) Parse ALL provided files fully:
   - For PUML: packages, classes, enums, attributes, operations, associations (including multiplicities), generalization, composition/aggregation, notes, stereotypes, tagged values.
   - For Requirements Doc (if provided): extract explicit module/configuration requirements and map them to MCCL configuration parameters/constraints where appropriate.

2) Build a unified symbol table across files.
   - The Domain PUML decides the domain classes/enums/relations.
   - The Meta-model PUML decides what MCCL constructs, annotations (if any), and parameters you are allowed to emit.
   - The Requirements Doc may add module/configuration constraints for MCCL elements already present in the configuration model (do NOT introduce new domain concepts).

3) Generate Java (ONE FILE):
- Generate MCCL configuration classes based on the MCCL meta-model.
- Each module defined in the PUML must produce one configuration class.
- Configuration classes typically describe:
  - module name
  - domain model class
  - controller class
  - view class
  - application behaviour configuration
  - menu structure
  - data source configuration
  - other module-level settings defined by the MCCL meta-model.

- The generated code must reflect the structure of the MCCL meta-model PUML:
  - classes representing modules
  - configuration attributes
  - relationships between modules and domain models
  - controller/view bindings.

- Do NOT generate domain entity classes.
- Do NOT generate Java for domain classes/enums; only reference them (e.g., as Class literals or type names) where MCCL requires bindings.
- Only generate configuration/module classes defined by the MCCL meta-model.


4) Minimal comments requirement:
   - If a rule came from Requirements Doc:
     // [REQ] from <requirements doc path>: <rule text>
   - If inference is used:
     // [INFERRED] reason: ...
   - Only include comments when they add value, limited to:
     // [REQ] ... (requirements doc rule)
     // [INFERRED] ... (only when needed)
     // [DEFERRED/UNMAPPED] ... (only when needed)

5) Do NOT invent domain concepts not in the Domain PUML.
   - Do NOT generate any scaffolded classes/files (e.g., ConstraintViolationException). If a type is missing from the PUML inputs, reference it but do not create it; instead mark the usage:
     // [DEFERRED/UNMAPPED] Missing type definition: <TypeName>

6) If a required MCCL configuration element cannot be mapped to constructs available in the MCCL meta-model, still generate Java code but:
   - add a comment block: // [DEFERRED/UNMAPPED] ... why

7) Formatting must match the example style closely:
   - Annotation placement, indentation, section headers, method stubs, and field visibility.

IMPORTS (must be correct and complete):
- Include ALL required imports so the file can compile assuming the MCCL library/runtime is on the classpath.
- The model MUST analyze the entire generated code (fields, method parameters, return types, collection instantiations, counters, etc.) and import every concrete Java type that appears anywhere in the file.
- If Collection is used → import java.util.Collection.
- If ArrayList is used → import java.util.ArrayList.
- If List is used → import java.util.List.
- If Set is used → import java.util.Set.
- If HashSet is used → import java.util.HashSet.
- If Double, Integer wrapper types are used → no import required.
- If any util class is referenced explicitly, it MUST be imported explicitly (do NOT rely on wildcard imports).
- Wildcard imports (e.g., java.util.*) are NOT allowed.
- Include imports for all MCCL configuration classes, annotations, and enums required by the generated configuration code.
- Determine the import package names by:
  1) Using explicit package names found in the Meta-model PUML (preferred).
  2) If Meta-model PUML does not provide package names, use a single inferred base package for MCCL imports, and mark it:
     // [INFERRED] MCCL import base package assumed: <package>
  3) Do NOT omit MCCL imports. Do NOT rely on default package.
After generating each section, re-evaluate the section and verify that every referenced type has a corresponding import statement. If any type lacks an import, add it before finalizing the output.

ASSUMPTIONS you may make (but must annotate as [INFERRED] when used):
- Default mutable=true for classes unless meta-model says otherwise or PUML indicates immutability.
- Default optional=false when multiplicity lower bound is 1; optional=true when 0.
- For multiplicities in MCCL configuration relationships, follow the MCCL meta-model’s parameters for cardinality if provided; otherwise annotate [INFERRED].
- If a module name or configuration identifier is missing, generate a deterministic name derived from the module or bound domain type (e.g., "<DomainType>ModuleConfig" or "<ModuleName>Config") and annotate [INFERRED].
- If Requirements Doc is ambiguous, choose the most standard and safe validation rule and annotate [INFERRED].

Now process the following input files, and output ONLY the Java file, nothing else.

INPUT FILES:
- Domain PUML: <path + content>
- Meta-model PUML: <path + content>
- Requirements Doc (optional): <path + content or extracted text>